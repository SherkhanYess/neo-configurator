Checkbox (default) or radio (`type="radio"`) with label.

```jsx
<Checkbox label="Согласен с условиями" defaultChecked />
<Checkbox type="radio" name="metal" label="Белое золото" />
<Checkbox type="radio" name="metal" label="Платина" />
```

Use `name` to group radios. Both share the ice accent fill.
