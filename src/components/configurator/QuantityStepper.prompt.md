Compact −/＋ stepper for quantities in cart lines and order summaries.

```jsx
<QuantityStepper defaultValue={1} min={1} max={10} onChange={setQty} />
```

Clamps to `min`/`max` and disables the relevant button at the bounds.
