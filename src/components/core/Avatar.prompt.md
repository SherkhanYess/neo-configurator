Round avatar with image or initials fallback.

```jsx
<Avatar name="Анна Кей" src="/anna.jpg" status />
<Avatar name="Studio Manager" size="sm" />
```

Sizes `xs`/`sm`/`md`/`lg`. Omit `src` to show initials derived from `name`. `status` adds an online dot.
